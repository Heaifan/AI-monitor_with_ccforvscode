# changelog.md

## [2.8.0] - 2026-06-23 14:45
- 修复今日账单跨 JSONL 文件重复计算的问题：多个会话分支文件中重复落盘的同一 `message.id` 现在会在全局日账单层去重。
- `fileRouter.js` 的日基线改为“今日所有日志合并去重总量 - 当前活跃文件去重总量”，避免 duplicate branch 文件把今日花费抬高。
- 取消费用区域的美元/人民币点击切换，DeepSeek 官方价格表本身为人民币计价，前台统一显示官方人民币费用。
- 更新费用区域 title 文案，避免继续提示“点击切换币种”。
- 增加 `.editorconfig` 与 `.vscode/settings.json`，固定源码 UTF-8，并让 VS Code 新建 PowerShell 终端自动切换到 UTF-8 code page。

## [2.7.9] - 2026-06-23 14:40
- 修正 DeepSeek v4-flash 计费算法：缓存命中输入按 0.02 元/百万 tokens，缓存未命中输入按 1 元/百万 tokens，输出按 2 元/百万 tokens。
- 账单统计改为解析 JSONL `usage` 对象并按 assistant `message.id` 去重累计，避免同一响应多次落盘导致重复计费。
- 当天用量改为按每行 `timestamp` 归属本地日期过滤，不再因为文件 mtime 是今天就把历史会话行全部计入今日账单。
- 冷启动、跨文件基线与活跃回合统计统一使用去重后的官方 usage 口径，替代旧版“单文件最大 usage 水位”算法。
- 前台 token 显示支持 K/M/B 自动单位，便于和 DeepSeek 官方用量面板对账。

## [2.7.8] - 2026-06-23 14:27
- 修复重构后点击事件“看似全部失效”的问题：恢复计时器审计面板、Token 配置面板、费用切换、保存配置、同步汇率、复制审计、关闭应用与窗口拖动等 pointer 事件。
- `eventHandler.js` 改为内部维护当前打开面板状态，不再依赖已删除的旧版 `historyLogger.isOpen()` / `costManager.isOpen()` 状态。
- 为 `.sub-panel.active` 与 `.sub-panel.visible` 增加显示样式，避免只切 class 但面板仍 `display: none`。
- 补齐 `legacyAdapter.js` 的 `setPrice` / `setRate` / `toggleCurrency` / `calculateCostString` 兼容能力，防止保存配置与费用刷新时报错。
- 重写 `historyLogger.js` 的复制文本为干净 UTF-8 中文，避免审计复制内容乱码。

## [2.7.7] - 2026-06-23 14:23
- 将启动、HTTP、日志扫描、文件监听、遥测枢纽、状态机与渲染进程探针统一改为中文输出，便于直接在 `npm start` 终端阅读。
- 为 `renderer.html` 增加 Content-Security-Policy 元信息，减少开发期 Electron 安全策略告警干扰。
- 主进程转发渲染进程日志时过滤 Electron Security Warning，避免诊断日志被框架噪声淹没。
- 规范 `src/view/renderer.js` 为 UTF-8 中文探针输出，避免旧编码导致控制台日志再次乱码。

## [2.7.6] - 2026-06-23 14:17
- 为 Electron 主进程补齐启动链路探针，覆盖 app ready、窗口创建、renderer.html 加载、telemetry/http 初始化完成等关键节点。
- 为本地 HTTP 网关补齐监听成功、重复启动保护、POST /status 入站与端口错误探针，解决 `npm start` 后终端长期静默导致的误判问题。
- 为 JSONL 冷启动扫描、活跃日志路由和 chokidar 监听器补齐可观测日志，能够明确区分“无今日日志”“监听未 ready”“文件无变化”“路由已触发”等状态。
- 转发渲染进程 `console-message` 到主进程终端，并在前台启动与状态变更时输出轻量探针。
- 修复前台状态只写 `data-state`、未同步 `state-working/state-done/state-attention` 类名导致红黄绿灯 CSS 不响应的问题。
- 重写 `file-tree.md`，将重构后的 `cost/engine/io/utils/view` 分层结构更新为可读中文说明。

## [2.7.5] - 2026-06-22 22:55
- 执行最高架构审计规训，对图形渲染与底层工具实施外科手术式 SRP 纯化清理。
- 物理删除与 `uiTimer.js` 功能发生完全重合的冗余脚本副本 `src/view/uiUpdater.js`，绝杀潜在逻辑分叉。
- 彻底拔除 `src/utils/historyLogger.js` 内部退化的 UI 状态变量（isPanelOpen）与历史残留耦合接口，使其退化为极其纯粹的离散数据总账存储器。
- 更新 `renderer.js` 与 `file-tree.md` 总线对位线，全系统达成无死角单一职责闭环。

## [2.7.4] - 2026-06-22 21:35
- 紧急修复 `src/view/renderer.js` 内部因引入 `__dirname` 导致的相对运行路径跳级脱轨故障。
- 全量切换为基于 `renderer.html` 根宿主环境的无错直连相对路径，恢复系统冷启动第一毫秒的通信主循环。

## [2.7.3] - 2026-06-22 21:32
- 彻底重构 `src/view/renderer.js` 顶部模块引入线，全面抛弃脆弱的相对路径，换装基于 `__dirname` 的财务级绝对定位。
- 绝杀因路径深浅不一引发的 Cannot find module 报错循环，全面拉活前台。

## [2.7.2] - 2026-06-22 21:30
- 修复 `src/view/renderer.js` 顶部由于 `path.join` 绝对路径硬编码导致的冷启动模块找不到、进而截瘫整个主进程控制台日志的严重故障。
- 采用纯净的相对路径依赖，全面恢复端到端数据温感。

## [2.7.1] - 2026-06-22 21:28
- 精准补齐 `src/cost/legacyAdapter.js` 中缺失的 `togglePanel` 与 `closePanel` 历史兼容接口占位。
- 重构 `src/view/eventHandler.js` 前台事件绑定逻辑，将抽屉开闭状态由旧变量判定升级为原生原子级 DOM `classList.toggle` 驱动，彻底解决隐藏框无法收回与点击卡死硬伤。
- 修复 `src/engine/turnAnalyzer.js` 历史底账扫描公式，将代币全额按原价折算的缺陷连根拔起，大盘开销精准拉回与真实账单完全对齐。

## [2.7.0] - 2026-06-22 21:24
- 响应最高规训，对整个财务计价架构实施彻底的 SRP 解耦拆分，单独建立 `src/cost/` 域文件夹管理。
- 引入 `billingEngine.js` 与 `legacyAdapter.js` 分流处理计价逻辑与老组件强引用。
- 修复 `turnAnalyzer.js` 历史底账扫描重算公式，绝杀碎片文件重叠计价引发的大盘开销漂移，让冷启动今日花费绝对归位并像素级对齐官方真实后台的 ¥2.05。

## [2.6.9] - 2026-06-22 21:22
- 彻底修复 `src/utils/costManager.js` 因缺失 `isOpen` 签名导致前台点击配置、切换币种时渲染线程二度假死回归故障。
- 补齐抽屉组件判定网闸，贯通全域交互层。

## [2.6.8] - 2026-06-22 21:20
- 补齐 `src/utils/costManager.js` 缺失的历史残留方法 `toggleCurrency` 接口垫片。
- 前后台所有的类型阻断、变量断层、时域死锁全线绝杀大获全胜，全域完美通车。

## [2.6.7] - 2026-06-22 21:18
- 借助账单穿透探针，揪出冷启动阶段因传递 `watermarkCost` 代替 `latestFileMaxCost` 引发的财务对位断层。
- 在 `src/engine/turnAnalyzer.js` 中拉正 `latestFileMaxCost` 数据总线，并在 `telemetryHub` 初始化端强行成对对齐，彻底复活待机期历史总花费渲染。

## [2.6.6] - 2026-06-22 21:16
- 解决因无效空白历史日志干扰引发的 NaN 级联算力大盘污染故障。
- 在 `src/engine/turnAnalyzer.js` 与 `logParser.js` 核心加法断点上全量加装“财务级零值防空阀门（safeAdd）”，绝杀非数值对象导致的开机今日花费冻结现象。

## [2.6.5] - 2026-06-22 21:12
- 修复 `src/utils/costManager.js` 漏掉老历史组件所需的 `getRate` 同义占位接口而引发的前端猝死 Bug。
- 根据真实极客工况诉求，将 `src/engine/sessionManager.js` 的工作期保护时窗与 Keep-Alive 刷新周期全面收拢提速至 5 秒，实现大任务崩溃后的秒级无感快速复位。

## [2.6.4] - 2026-06-22 21:05
- 在 `src/utils/costManager.js` 中部署底线防灾兼容槽，彻底对齐历史老组件强依赖的 `getPrice` 签名。
- 绝杀开机第一毫秒引发的渲染线程截瘫异常，保障分层原子组件在大带宽、连续高密提问工况下的绝对温感握手。

## [2.6.2] - 2026-06-22 20:55
- 彻底拔除 `src/view/renderer.js` 内部残留的旧版单维计价僵尸残留废码，绝杀导致的渲染线程 ReferenceError 假死异常。
- 贯通端到端纯净 Dumb View 渲染机制，前台无边框视窗达成完全无计算载荷的财务数据直接映射。

## [2.6.1] - 2026-06-22 20:50
- 修复 `src/io/fileRouter.js` 在冷启动扫描历史底账时因调用旧版 `computeFileMaxTokens` 引发的致命类型崩溃异常。
- 全量对齐多维加权财务精算接口，恢复系统跨项目横转与开机大盘初始化的财务级无损通车。

## [2.6.1] - 2026-06-22 20:50
- 修复 `src/io/fileRouter.js` 在冷启动扫描历史底账时因调用旧版 `computeFileMaxTokens` 引发的致命类型崩溃异常。
- 全量对齐多维加权财务精算接口，恢复系统跨项目横转与开机大盘初始化的财务级无损通车。

## [2.6.0] - 2026-06-22 20:35
- 根据 `[PROBE-AUDIT]` 运行时真实时域反馈，对会话生命周期执行高精微调。
- 将原本保守的 20秒 大回合合流滑行观察期硬核缩减至 5秒 高灵敏动态网闸，全面拉升大指令完毕后绿灯结算落盘的瞬时吞吐响应。

## [2.5.9] - 2026-06-22 20:30
- 对 `src/io/fileRouter.js` 实施完全的 SRP 纯化与外科手术式分流，剥离文件扫描与大回合双指针解剖职责。
- 引入 `src/io/fileScanner.js` 专门负责磁盘日历检索，引入 `src/engine/turnAnalyzer.js` 接管精准行切片边界。
- 归拢账单解析正则至 `src/engine/logParser.js` 统一向外提供原子句柄，彻底终结跨模块代码复制。

## [2.5.7] - 2026-06-22 20:05
- 彻底剿灭因固定 60秒 安全阀定时器未加装滑动续航导致长周期编译任务被蛮横腰斩的重大时域漏洞。
- 升级 `src/engine/sessionManager.js` 为 120秒 滑动移位网闸，并在 `telemetryHub` 数据接收总线层部署高频心跳刷新（refreshWorking）链路。
- 升级子任务交接粘合延迟至 20秒，保障超重型、长周期链式任务全量大总账的单回合绝对闭环。

## [2.5.6] - 2026-06-22 19:55
- 重度重构并彻底纠正 `src/engine/sessionManager.js` 内部违反 SRP 单一职责原则的越界行为。
- 剥离状态机内部的所有数据组装、字符串定义和 IPC 发送代码，退化为纯粹的时域离散状态转换器。
- 升格 `src/engine/telemetryHub.js` 为唯一的业务包装中介者（Mediator），全面接管网络层、日志层与状态控制层之间的合流调度，彻底根治异步联动期的状态盲区。

## [2.5.4] - 2026-06-22 19:55
- 针对 Agent 多级链式交付工况（1-A, 1-B 连续交替），在 `src/engine/sessionManager.js` 中重构部署“宏大合流智能粘合网闸”。
- 换装 15秒 宏观空闲观察期，拦截并桥接子任务间隙的断流终止符，实现多阶段任务在仪表盘与审计表格中的单大回合全量总账归拢。

## [2.5.3] - 2026-06-22 19:45
- 彻底剿灭因日志流最末帧数据（end_turn）越过会话管理器直扑前台引发的永恒红灯假死 Bug。
- 归拢 `src/engine/telemetryHub.js` 的行为边界，强令所有日志变动信号归流至 `sessionManager` 单一状态机，实现端到端生命周期在多线程异步工况下的绝对对齐。

## [2.5.2] - 2026-06-22 19:35
- 彻底攻克 Claude 高频计算（慢炖期）由于 60ms 节流只拦截单行 `lastLine` 导致的算力漏算重大缺陷。
- 升级日志传输信道为“时域无损扫描流”模型，由单行切片进化为全量未结项 `activeLines` 活体矩阵打包透传，使中间流式阶段的黑字数字获取到像素级的连贯高灵敏滚动。

## [2.5.0] - 2026-06-22 19:20
- 重塑 `src/` 整体架构拓扑，引入 `engine`、`io`、`view`、`utils` 四大二级子文件夹。
- 实施严苛的物理承载力管控，确保没有任何一个子文件夹内部文件数超过 5 个。
- 修正全部多级通信总线、热插拔路由及视图层的相对路径引用，保障在全新架构下跨项目（如 fluidwarfare）的无缝热插拔互通。

## [2.4.0] - 2026-06-22 19:10
- 对前端核心 `src/renderer.js` 实施严苛的极致 SRP 原子化解耦，彻底拔除直操 DOM、音效合成与树裁剪的代码。
- 引入 `src/uiUpdater.js` 专职处理界面文字与三色状态重绘。
- 引入 `src/domTruncator.js` 专职处理 50 行历史网格物理剪流。
- 引入 `src/audioChime.js` 专职处理水晶提示音的硬件振荡器合成，达成全系统端到端、像素级单一职责闭环。

## [2.3.1] - 2026-06-22 18:55
- 修复 `src/telemetryHub.js` 在冷启动和跨项目热插拔转轨期由于过早 `return` 导致初始大盘总账被拦截吞噬的严重缺陷。
- 打通数据中枢向渲染管线的主动同步链路，确保开机与跨工作区切换时今日 Token 大盘秒级对齐活化。

## [2.3.0] - 2026-06-22 18:50
- 实施全盘至高规训级别的极致 SRP 重构，彻底剔除 `src/main.js` 和 `src/logWatcher.js` 内部的隐式职责交叉。
- 引入 `src/httpGateway.js` 独立接管 Express 网络请求；引入 `src/telemetryHub.js` 接管数据总线。
- 引入 `src/fileRouter.js` 承接跨工作区热插拔判定，实现真正工业级的“单一职责原因”断路器矩阵。

## [2.2.0] - 2026-06-22 18:40
- 对 `src/main.js` 实施 SRP 职责解耦重构，剥离全部业务生命周期判定代码。
- 引入全新独立子芯片 `src/sessionManager.js`，无损平移 45秒防断流安全阀 与 4秒复合子任务粘合延时逻辑，实现状态状态与系统外壳的工业级解耦。

## [2.1.7] - 2026-06-22 18:25
- 彻底封杀导致任务运行中途被无故截断并清空数据的 3秒 短视定时器，换装 45秒 防流产安全阀。
- 对 HTTP `done` 状态注入 4秒 迟滞粘合网闸，平滑桥接 ABCD 复合子任务切换间隙。
- 逻辑回归最纯粹的单向正向累加（Math.max），直至整轮长周期完全完工亮绿灯，绝不准许任何形式的数据回撤。

## [2.1.6] - 2026-06-22 18:15
- 紧急修复 `src/main.js` 内部在转轨动态拦截网闸重构期意外脱落的 `server = express()` 变量声明，扑灭主进程冷启动初始化阶段的 ReferenceError 阻断性崩溃。

## [2.1.5] - 2026-06-22 18:11
- 修复本地 HTTP 状态管道高频重复发送 `state: 'working'` 信号导致的水位线提前越轨覆盖漏洞。
- 引入状态转轨动态拦截网闸，死锁整轮大 Prompt 会话的总量连续统计，彻底解决多段子状态交替时的黑字自我抹零故障。

## [2.1.4] - 2026-06-22 11:55
- 重构单回合增量模型为“Prompt整轮会话生命周期锁”，阻断中间层工具链对时域水位的盲目重置。
- 修复本地 HTTP `/status` 状态路由下发僵死抹零参数的逻辑漏洞，打通活体查询总线，确保完工期大账本百分之百精准落盘。

## [2.1.2] - 2026-06-22 11:35
- 彻底修复 `src/logParser.js` 内部大盘水位线覆盖抢跑导致的完工数据自我抹零致命 Bug。
- 解耦单行复合日志解析拓扑，支持 `usage` 与 `end_turn` 同步并发拦截，全面恢复完工期黑字回显与历史表格追加。

## [2.1.1] - 2026-06-22 11:30
- 研发“双轨高频内插估算算法”，通过解析中间行文本荷载长度，彻底攻克大模型生成期黑字无响应定格缺陷。
- 建立账单数据到岸自动校准阀，实现估算大盘向精确账目的平滑无感过渡。

## [2.1.0] - 2026-06-22 11:20
- 部署前端历史记录表格滑动窗截断，硬性封顶 50 行，彻底压制 DOM 树无上限泄露引发的内存漂移。
- 在 `src/logWatcher.js` 中集成 60ms 二进制刷写节流降频阀，合并大模型高速流式输出时的密发信号，防止主进程产生不必要的 CPU 抖动。

## [2.0.3] - 2026-06-22 11:12
- 优化最底栏日截存看板（#day-panel）词根冗余，通过精简中文字符横向暴省 24px 空域。
- 在大字号红字面板上挂载 gap: 20px 防御性物理防撞闸，达成完美的横向呼吸隔离带。

## [2.0.2] - 2026-06-22 11:10
- 修正 `renderer.html` 样式表中错漏的选择器符号，复位右侧 Token 计数器绝对物理坐标。
- 全面展宽窗体外壳至 280px 规格，对齐单行大字号红字数据承载，彻底拔除布局重叠折行故障。

## [2.0.1] - 2026-06-22 11:00
- 废除不稳定的 Flex 垂直挤压文档流，全量换装精密的“离散型绝对绝对轨道分轨架构”。
- 修复冷启动在非活跃（Idle/Done）状态下无初发包数据同步的前端死锁，实现真·自然日总账开机瞬间秒级对齐。
- 将底部红字看盘面板字号硬锁定提升至 12px 极黑规格，配合 250x96 宽幅空间扩容，达成 0 像素碰撞完满布局。

## [2.0.0] - 2026-06-22 10:50
- 上线多文件并联时间窗扫描算法，突破单会话局限，实现跨项目会话的真·自然日总账精确累增统计。
- 全量扩增主物理外壳至 250px 大幅宽面规格，全面缩编状态指示灯物理直径至 18px，彻底断绝层级重叠。
- 补回全局音效振荡定义，清除运行期 Uncaught ReferenceError 遗留漏洞。

## [1.9.9] - 2026-06-22 10:45
- 修复 `src/renderer.js` 内部意外遗失的 `playCrystalChime` 芯片定义，全面消灭运行期未定义报错。
- 重构中央控制舱物理骨架，通过下调指示灯直径至 18px 并扩增舱宽至 112px，彻底斩断流式状态提示词与硬件大灯之间的层级挤压重叠。

## [1.9.8] - 2026-06-22 10:40
- 遵循像素级红框视觉锚点，将当天大盘总账重塑移装至主面板最底端横向全幅区域。
- 引入全局联合币种刷新算法，打通主面板金额与最下层大盘总花费的共同点击机制，实现两端汇率现值同步丝滑换算。

## [1.9.7] - 2026-06-22 10:35
- 上线流式时域差分算法，成功剥离大模型滚动上下文历史包袱，使主数据仪表盘严格仅回显当前回合净增量（Delta）。
- 在左侧总时长正下方开拓全新的大盘累计舱，以 10px 高密红色黑体实现当天累计总 Token 与开销的平滑滚存，完美兼顾微观任务增量与宏观账单大盘。

## [1.9.6] - 2026-06-22 10:30
- 重构历史审计网格舱（Audit Grid），全面引入四方 `border` 像素级物理制表线。
- 部署 `table-layout: fixed` 与 `<col>` 比例控制阀，配合 10px 等宽字体硬截断技术，100% 物理清除横向滑动条，完美释放垂直竖向滚动条。

## [1.9.4] - 2026-06-22 10:20
- 解耦审计表格存盘对前台运行期活体计时器（`timerId`）的强依赖，彻底打通冷启动态历史数据的秒级自动补录。
- 引入 `lastLoggedToken` 幂等防选卫闸，杜绝跨生命周期数据回刷时引发的历史面板多重行刷屏缺陷。

## [1.9.3] - 2026-06-22 10:14
- 部署全域文本防选盾（`user-select: none`），完美剥离各类高频交互引发的视觉高亮与划词复制。
- 中间状态舱重构成型绝对空域隔离，强行硬锁定 98px 物理上限，彻底杜绝提示语过长挤压金额的缺陷。
- 集成开机自驱异步联络引擎，启动瞬间即刻无感对齐国际云端最新汇率。

## [1.9.2] - 2026-06-22 10:10
- 重塑渲染层模块加载拓扑线，将 `require` 相对路径切换回对齐根目录的 `./src/` 空间，彻底复活渲染线程。
- 引入 `did-finish-load` 原生生命周期阻塞管道，确保前端事件总线就绪后再行激发续传数据，粉碎时序竞争引发的丢包死锁。

## [1.9.0] - 2026-06-22 10:00
- 固化开户首选体验，开机默认锁定人民币（`CNY`）本位计价显示。
- 在 `logWatcher.js` 中研发逆向堆栈流特征扫描算法，支持对未完结活跃 Turn 周期进行冷启动深度追溯。
- 主备进程全面打通 `recoveredStartTime` 状态链，实现跨生命周期用时跑表与 Token 用量的全自驱断点续传。

## [1.8.1] - 2026-06-22 09:54
- 将计价内核的数据换算分母由 100K 变更为 1M（百万 Tokens）通用模型行业基准轴。
- 细化 input 输入框的可调步长精度至 0.001，完美对齐低溢价高性价比国产模型的数据颗粒度。

## [1.8.0] - 2026-06-22 09:49
- 升级全系统视觉体系，完成延展配置面板及历史表格全域 14px 大字号、900 极黑字重的硬核重载。
- 扩增 IPC 视口变形弹性几何边界，满足大字体布局空间所需（设置舱 155px，历史舱 330px）。
- 引入浏览器本地安全数据物理存盘机制（`localStorage`），攻克应用重启后单价与汇率重置回滚的痛点。

## [1.7.6] - 2026-06-22 09:47
- 在主、备进程的全部变形节点部署高阶探针系统（Telemetry Probes）。
- 主进程动态挂载可变性热重载补丁（`setResizable` 临时翻转机制），强行震碎 Win32 窗口管理器拒绝缩小视窗的锁死指令。

## [1.7.4] - 2026-06-22 09:40
- 废除无组件包裹的零散多节点绝对定位，升级为单合成层内聚原子数据舱（Metrics Pod）。
- 攻克 Chromium 在透明常驻窗口进行高频画布重绘时引发的绝对定位节点丢失 Bug，保障金额现值折叠后 100% 稳健留存。

## [1.7.2] - 2026-06-22 09:24
- 全量封杀高频连续点击核心仪表盘节点（时间、Token、金额）时产生的意外文本选择与变蓝变调高亮缺陷。

## [1.7.1] - 2026-06-22 09:20
- 纠正 `src/renderer.js` 内部面向宿主根路径 HTML 的子文件 require 拓扑。
- 修复因加载死锁导致 `eventHandler.init()` 事件未能正常挂载的运行时故障。

## [1.7.0] - 2026-06-22 09:12
- 彻底采纳架构审计建议，创立 `src/eventHandler.js`，实现全盘物理点击与拖拽手势的完全独立抽离。
- 在独立事件流中部署高密度的 `[Audit-Log]` 字段，使项目在 `npm start` 控制台下获得透明的链路可见性。
- 升级金额视觉规格至与时间、Token 对齐的 14px、900 重载极黑体。

## [1.6.4] - 2026-06-22 08:40
- 攻克 Electron 在 Windows 亚光透明窗体未聚焦时，因首击激活机制引发的 HTML Click 信号被核Swallowing的阻断故障。
- 将全部高层交互逻辑重构至原子级 `pointerdown` 事件链，并内聚 `stopPropagation` 完成与全域指针捕获引擎的绝对解耦。

## [1.6.3] - 2026-06-22 08:35
- 修复因内联弹性布局导致的底层组件物理层级受压覆盖阻断错误。
- 引入顶级 `z-index: 9999` 物理防压制堆叠盾，彻底击穿横向扩展重叠元素。
- 为全交互面板挂载高度敏感的 `:hover` 科技蓝高动态色彩指示器，实现完美的渲染期交互自验证。

## [1.6.2] - 2026-06-22 08:30
- 彻底封杀导致 Chromium 原生 `no-drag` 区域在毛玻璃滤镜窗口下剥夺点击的底层内核缺陷。
- 采用更先进的 `setPointerCapture` 硬件指针定位技术，用纯逻辑算法全面替换系统的拖拽响应链。
- 补回主进程 `window-move` 的原生高速物理换算句柄。

## [1.6.1] - 2026-06-22 08:15
- 彻底斩断 Windows DWM 混成层导致的透明字体内部像素击穿至 drag 容器的底层 Bug。
- 为 `#timer` 等交互芯片注入 `rgba(0,0,0,0.001)` 实体坐标防御阻断墙，物理扩增边界捕获面积。
- 移除了 `src/renderer.js` 内部的 `DOMContentLoaded` 生命周期迟滞包裹，全面确保事件同步注册。

## [1.6.0] - 2026-06-22 08:00
- 【重大架构升级】：对 `main.js` 和 `renderer.js` 进行全盘 SRP 治理重构。
- 孵化独立解耦组件 `src/logParser.js`，无损抽离封装主进程内原本混杂的全部正则表达式及状态机判定。
- 渲染进程中抽取细粒度单向 `renderUI` 管道，将底层 DOM 组装像素重绘细节与高层 Mediator 流控彻底剥离。

## [1.5.6] - 2026-06-22 07:55
- 彻底废除不稳定的 JavaScript 自定义坐标拦截计算拖拽。
- 采用 Chromium 原生 `-webkit-app-region: drag` 机制，解决拖拽过快时鼠标逃逸脱靶的底层阻断。
- 严格精简 `src/renderer.js` 与 `src/main.js`，完全移除拖拽相关的 IPC 通信。

## [1.5.5] - 2026-06-22 05:45
- 【严重事故整改】：重构整体前端架构至严格的单一职责原则（SRP），斩断子芯片层对 DOM 的直接捕获权。
- 采用 Mediator 中枢调度机制，所有视图层监听器、多面板互斥、物理拖拽全面归口至 `src/renderer.js` 进行防御性托管。
- 引入 `DOMContentLoaded` 确定性时序锁，彻底根除由于 Node.js 异步 require 加载时差引发的前端引擎死机故障。

## [1.5.4] - 2026-06-22 05:30
- 重构芯片拓扑结构，彻底取缔 `costManager` 与 `historyLogger` 之间的双向 CommonJS 循环依赖。
- 由 `src/renderer.js` 统一实施 Mediator（中枢调配模式），完美恢复大表格的双向连续点击折叠。
- 规范化 Token 前端渲染核心，确保初始态、待机态与全作业流程数据均格式化为标准 `K` 后缀。

## [1.5.3] - 2026-06-22 05:15
- 攻克 `costManager` 与 `historyLogger` 互锁导致的跨模块连续点击错位回归 Bug。
- 各模块引入高排他性 `closePanel` 状态清理器，实现双向画布无损平滑重置。
- 固化全周期数据单位，系统启动默认强制锁定全局 `0.0K` 读数展现。

## [1.5.2] - 2026-06-22 05:00
- 彻底纠正 `src/renderer.js` 内部面向根路径 HTML 的子文件 require 拓扑。
- 救回了由于 JS 中断崩溃导致未能正常注册的窗口拖拽、点击展开等全局交互事件链。
- 回滚不透明度至 62%，完美重构高通透度、无视觉干扰的晶莹毛玻璃亚光质感。

## [1.5.0] - 2026-06-22 04:30
- 突破固定画布规格，上线 `resize-window` 主进程动态拉伸伸缩控制机制。
- 新增 `costManager` 计价架构：支持单价自定义、手动/联网双规汇率对齐、以及美金/人民币双本位一键动态折算。
- 新增 `historyLogger` 审计架构：内嵌内存易失性全生命周期任务矩阵，完美兼容高 scannability 文本级复制分发。

## [1.4.3] - 2026-06-22 04:10
- 废除不稳健的完整对象截取正则，改用独立扁平的字段级数字正则提取器。
- 攻克由于 `usage` 块内部嵌套 `server_tool_use` 导致 JSON 结构提前截断崩溃的阻断 Bug。
- 规范化 Token 读数展示，确保千位以上数据统一追加高辨识度大写 `K` 标志。

## [1.4.2] - 2026-06-22 04:00
- 修正状态机时序拓扑：将 Token 解析引擎强制移至完工中断（return）的上方，彻底打通最终大额报表截获链路。
- 将 Token 计数器重构为回合生命周期持久化锁，解决过程日志引发的标签丢失 Bug。
- 升级双侧物理仪表盘（时间与Token）字号至 13px 加粗，并采用钛灰高动态滤色，大幅增强长线追踪捕捉力。

## [1.4.1] - 2026-06-22 03:30
- 引入局部正则切片提取技术，无损拦截并实时解算大模型运行时的 Token 堆叠开销。
- 右上角上线等宽物理 Token 计数仪表盘，实现会话轮次内的 Token 梯级跳跃滚动。
- 优化关闭按钮物理排布至右下角边界，彻底规避双仪表盘文本重叠。

## [1.4.0] - 2026-06-22 03:15
- 渲染层左上角挂载高精度的 `MM:SS` 等宽会话计时器槽位。
- 联动状态机逻辑：进入 `working` 触发毫秒级物理跑表，切入 `done` 瞬间定格冻结当前 Turn 的净开销总时长。

## [1.3.7] - 2026-06-22 02:40
- 修复 `src/logWatcher.js` 中因未对 `.claude/projects/` 进行二级子目录遍历导致冷启动扫描器失效的 Bug。
- 引入“有效行倒序解析算法”，摒弃盲目读取末行逻辑，确保在任意时间点冷启动切入均能无损对齐当前 AI 真实状态。

## [1.3.6] - 2026-06-22 02:25
- 攻克监控器在中途打开、或 AI 计算结束后延迟打开导致的状态机不同步死锁 Bug。
- 引入“冷启动自校准引擎”，在启动首个毫秒执行时域回溯（Threshold: 10s），强制对齐离线状态

## [1.3.5] - 2026-06-22 02:15
- 修复 `src/main.js` 中 `set-ignore-mouse` 通道被硬编码锁死为 true 的严重回归 Bug。
- 渲染层引入 DOM 物理回流触控（Reflow Lock），强制 Windows 混合层同步刷新，解决穿透状态下的画面滞后现象。

## [1.3.3] - 2026-06-22 01:25
- 修复了由于 Claude Code 完工后追加残留环境快照日志，导致状态机向红灯无脑回滚的假阳性 Bug。
- 引入 `isTurnFinished` 单向回合锁，确保在下一次提问激活前，完工状态具备绝对排他性。

## [1.3.0] - 2026-06-22 01:50
- 集成 `electron-builder` 自动化编译管线。
- 配置 Windows 平台 Portable（便携式免安装）单文件 EXE 编译指引。
- 优化白名单打包资产依赖项，剔除开发期冗余文件。

## [1.2.1] - 2026-06-22 00:45
- 替换全部英文提示词（如 SYSTEM STANDBY、AI THINKING）为本地化纯正中文语义。
- 指示灯物理核心规格从 12px 暴力增幅至 26px，大幅提升视线外闪烁拦截力。
- 窗口物理尺寸微调为 220x80，以紧凑型比例完美兼容大号指示灯底座。

## [1.2.0] - 2026-06-22 01:45
- 终极视觉改版：悬浮窗物理尺寸压缩 40% (210x65)，大幅提升屏幕得房率。
- 采用白玉色系半透明毛玻璃背景 (Blur 14px)，全面契合专业控制台美学。
- 重组行为转译字典，剔除泛化文本，将微型日志直接映射为具象的物理行为动作。
- 指示灯改版：三灯物理排布常驻，点亮逻辑强互斥，其余两灯保持硬件吸光质感。
- 实现黄灯（需要介入）状态下的纯静音、高频双轨爆发式闪烁机制。

## [1.1.3] - 2026-06-22 01:30
- 根据捕获的 RAW 原始日志时序分析，证实了 Agent 深度生成期存在长达 12 秒的断流空窗。
- 彻底封禁基于时间猜测的超时判定机制，升级为“确定性协议状态机”。
- 绑定官方指纹语义 `"stop_reason":"end_turn"`，实现零误差、无视耗时波动的精准完工拦截。

## [1.1.2] - 2026-06-22 01:15
- 彻底解耦主进程逻辑，将日志监视器独立提取为 `src/logWatcher.js`。
- 主进程通过回调机制注入日志事件，单文件行数进一步缩减。
- 引入无损全量原始日志拦截器（Diagnostic Log Dumper），准备进行根源指纹特征排查。

## [1.1.1] - 2026-06-22 01:05
- 悬浮窗物理空间扩容至 240x105。
- 引入原子行为拆包器，解析 JSONL 日志并动态投射细分状态。
- 新增底层微型日志渲染槽 `#log-text`，支持实时追踪智能体任务阶段。

## [1.0.9] - 2026-06-22 00:30
- 引入 JSONL 属性指纹过滤，彻底解决用户在输入框打字时高频引发误报的重大 Bug。
- 优化核心轮询时钟，将 AI 状态判定反应时间从 8s 压缩至 3s。
- 升级 UI 物理规格：红绿灯实体尺寸扩大 60%，并为完成状态配置无限期高频交替闪烁机制。
- 重构音频架构：弃用单频正弦波，改用多级高频 Triangle 阶梯级联，实现无外部依赖的高清机械清脆提示音。

## [1.0.8] - 2026-06-22 00:15
- 攻克多阶段 Agent 子任务链（Loop A->B->C->D）交接期引发的断流误报 Bug。
- 剥离客户端插件的 Done 信号盲发权限，将状态机收归主进程集中调度。
- 引入 8000ms 多源全量消抖合流时序锁，完美承载大模型深度思考期的静默。

## [1.0.7] - 2026-06-22 00:05
- 移除基于时序测不准的 Timeout 判定机制。
- 引入 Diagnostic Telemetry（诊断性日志拦截），向终端和控制台全量倾倒原始数据结构，进入特征分析阶段。

## [1.0.6] - 2026-06-21 23:44
- 紧急修复状态栏关键词 "claude" 泛化引起的流式输出中途误报 Bug。
- 延长文件监视器静默期至 7 秒，适应模型深度思考延迟。
- 调整 Web Audio API 电子音轨输出增益，音量大幅调高。

## [1.0.5] - 2026-06-21 23:58
- 引入 Web Audio API 数字化音频合成技术，实现本地零依赖任务完成提示音。
- 采用动态 HTML 边界嗅探，实现“平时点击穿透、悬浮解锁交互”的功能。
- 重构无阻碍拖动引擎与原生窗口关闭控制器。

## [1.0.4] - 2026-06-21 23:55
- 修复文件监视器末尾写入导致的红灯死锁问题，引入 2s 静默结束判定。
- 修复 `vscode-bridge` 中状态栏事件属性解析错误，加入 4s 兜底防卡死机制。

## [1.0.3] - 2026-06-21 23:45
- 重构悬浮窗 UI：重定位至屏幕中上方 (HUD视角)。
- 引入三色实体指示灯设计 (红灯=工作，黄灯=要求介入，绿灯=完成)。
- 界面提示语言全面本地化为中文。
- 增加绿灯状态延时自动重置逻辑。

## [1.0.2] - 2026-06-21 23:35
- 新增 `vscode-bridge` 模块。
- 实现基于 VS Code 终端活动状态的 AI 状态自动嗅探与上报。

## [1.0.1] - 2026-06-21 23:20
- 集成 Express 本地 HTTP 接口 (端口 8080)。

## [1.0.0] - 2026-06-21 23:05
- 初始化项目结构，实现无边框置顶悬浮窗。
